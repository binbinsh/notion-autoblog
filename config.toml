baseURL = 'https://train.sh/'
languageCode = 'en-us'
title = 'train.sh Doppio'
theme = 'hugo-trainsh'

# Build settings
buildFuture = true
buildDrafts = false
timeZone = 'Asia/Shanghai'

# URL normalization
disablePathToLower = false
removePathAccents = true
preserveTaxonomyNames = false

[params]
  author = "Binbin Shen"
  mainSections = ["posts"]
  description = "Doppio is a double-shot helper for everyday coding workflows—small, focused, and practical."

[params.upvote]
  enabled = true
  endpoint = "/api/upvote"
  infoEndpoint = "/api/upvote-info"
  cookieDomain = ""

[params.social]
  email = "hello@train.sh"

[params.home]
  intro = """Building **Doppio** — a double-shot helper for everyday coding workflows. Small, focused, and practical.
<br>"""

[params.home.tags]
  # Set to 0 or negative to show all tags
  limit = 6

[params.home.recent]
  limit = 15

[outputs]
  home = ["HTML", "RSS", "JSON"]

[markup]
  [markup.goldmark]
    [markup.goldmark.parser]
      [markup.goldmark.parser.attribute]
        block = true
    [markup.goldmark.renderer]
      unsafe = true

  [markup.highlight]
    noClasses = false
    lineNos = false
    lineNumbersInTable = false
    anchorLineNos = false
    codeFences = true
    guessSyntax = true
    style = "tokyonight-day"
    styleDark = "tokyonight-night"

[build]
  [build.buildStats]
    enable = true

[caches]
  [caches.assets]
    dir = ':resourceDir/_gen'
    maxAge = -1
  [caches.getcsv]
    dir = ':cacheDir/:project'
    maxAge = -1
  [caches.getjson]
    dir = ':cacheDir/:project'
    maxAge = -1
  [caches.getresource]
    dir = ':cacheDir/:project'
    maxAge = -1
  [caches.images]
    dir = ':resourceDir/_gen'
    maxAge = -1
  [caches.modules]
    dir = ':cacheDir/modules'
    maxAge = -1

[[module.mounts]]
  source = 'assets'
  target = 'assets'

[[module.mounts]]
  source = 'hugo_stats.json'
  target = 'assets/watching/hugo_stats.json'
